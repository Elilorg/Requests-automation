3 fonctions principales : 
    -recuperer_toute_les_pages() : telecharge tout les pages du site et les met dans le dossier pages
    -recuperer_tout_les_numéros() : inscrit tout les numéros dans le fichier test.json à partir des fichiers telechargés
    -recupérer_messages_telephone(i) : récupére tout les messages de la page 1 du telephone à l'index i du fichier test.json

utilise selenium à cause du java_script dans la partie "récupérer les messages"

pourrais etre améliorer en utilisant une database SQL ou avec de la POO (à voir)

